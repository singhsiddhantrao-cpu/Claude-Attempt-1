agno>=2.7.0
anthropic>=0.40.0
# Required to use NVIDIA's free OpenAI-compatible models (set NVIDIA_API_KEY).
# Agno's Nvidia model class talks to build.nvidia.com through the openai client.
openai>=1.40.0
# Required to use Google's free Gemini models (set GOOGLE_API_KEY).
google-genai>=0.3.0
sqlalchemy>=2.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
python-multipart>=0.0.9        # required by AgentOS's form routes
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
openinference-instrumentation-agno>=0.1.0

# Required by the multi-agent stock analysis service (stock_service.py):
# free market data, news, and deterministic technical indicators.
yfinance>=0.2.40
pandas>=2.0.0

# Only required by claude_code_agent_service.py (wraps Claude Code itself
# as an AgentOS agent). Omit if you only need app.py.
claude-agent-sdk>=0.2.0

# Optional: sends traces to theagentos.space, a separate third-party SaaS
# unrelated to Agno's AgentOS above. Only activates if AGENTOS_API_KEY is set.
agenthog[anthropic]>=0.9.0

# Only required by census_agent.py (offline country-name resolution).
pycountry>=23.12.11
